<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/height_small"
    android:layout_gravity="center"
    android:background="?attr/selectableItemBackground"
    android:orientation="vertical"
    android:theme="@style/drawerTheme">

    <View
        android:id="@+id/highlight"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/padding_tiny"
        android:layout_marginTop="@dimen/padding_tiny"
        android:background="@color/nav_highlight"
        android:visibility="gone"
        tools:visibility="visible"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/padding_medium"
        android:paddingRight="@dimen/padding_medium">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/height_xxtiny"
            android:layout_height="@dimen/height_xxtiny"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/cd_navigation_icon"
            tools:src="@drawable/vic_event_black_24dp"
            tools:tint="@color/white"/>

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/height_medium"
            android:maxLines="1"
            android:textColor="@color/selectable_text_black"
            tools:text="Schedule"
            tools:textColor="@color/white"/>

        <ImageView
            android:id="@+id/action_icon"
            android:layout_width="@dimen/height_xxtiny"
            android:layout_height="@dimen/height_xxtiny"
            android:layout_gravity="center_vertical|end"
            android:contentDescription="@string/cd_navigation_icon"
            android:tint="@color/vote_title_gray"
            app:srcCompat="@drawable/ic_launch_black_24dp"/>

    </FrameLayout>

</FrameLayout>